version: "3.8"

services:
  backend:
    image: ghcr.io/mattwillms/sticky-zebra-backend:latest
    environment:
      CORS_ORIGINS: ${CORS_ORIGINS}
    expose:
      - "8765"
    devices:
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    restart: unless-stopped

  frontend:
    image: ghcr.io/mattwillms/sticky-zebra-frontend:latest
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
