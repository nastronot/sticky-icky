version: "3.8"

services:
  backend:
    build: ./backend
    expose:
      - "8765"
    privileged: true
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
