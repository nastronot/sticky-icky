# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Tesseract.js runtime assets — copied from node_modules + downloaded once
# by scripts/setup-tesseract.mjs (postinstall). Regenerated on every fresh
# `npm install`, so no need to track them in git.
public/tesseract/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
